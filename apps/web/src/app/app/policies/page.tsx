import { ComingSoon } from '@/components/shell/ComingSoon';

export default function PoliciesPage() {
  return <ComingSoon title="Policies" note="The policy editor lands in the next build pass. Lecture, Quiz, and Lab are active and selectable when starting a session." />;
}
