import { ComingSoon } from '@/components/shell/ComingSoon';

export default function SettingsPage() {
  return <ComingSoon title="Settings" note="Profile, school, and notification preferences land in the next build pass." />;
}
