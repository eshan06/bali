import { ComingSoon } from '@/components/shell/ComingSoon';

export default function ReportsPage() {
  return <ComingSoon title="Reports" note="Patterns are conversation starters, not verdicts. Unlock and focus-minute reports land in the next build pass." />;
}
