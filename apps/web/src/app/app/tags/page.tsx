import { ComingSoon } from '@/components/shell/ComingSoon';

export default function TagsPage() {
  return <ComingSoon title="Tags" note="NFC tags + printable QR — students tap either to start focus. Tag cards and print sheets land in the next build pass; the seeded desk tags already resolve in the iOS app." />;
}
