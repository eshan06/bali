import { ComingSoon } from '@/components/shell/ComingSoon';

export default function LogsPage() {
  return <ComingSoon title="Logs" note="The cross-class event stream lands in the next build pass. Recent activity is already on Home." />;
}
