import { Landing } from './landing';

/** Marketing landing — full §05 page; behavior ports design doc 06's frozen-timeline-
 *  safe reveal pattern. The interactive pieces live in ./landing.tsx (client). */
export default function Page() {
  return <Landing />;
}
